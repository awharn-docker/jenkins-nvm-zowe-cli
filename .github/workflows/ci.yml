name: ci
on:
  push:
    branches:
      - 'master'
  workflow_dispatch:

jobs:
  build-and-deploy-v1:
    runs-on: ubuntu-20.04
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Login to Dockerhub
        uses: docker/login-action@v2
        with:
          username: awharn
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        id:   build
        uses: docker/build-push-action@v4
        with:
          push: true
          build-args: |
            ZOWE_VERSION=zowe-v1-lts
          tags: |
            awharn/jenkins-nvm-zowe-cli:zowe-v1-lts
            ghcr.io/awharn/jenkins-nvm-zowe-cli:zowe-v1-lts
      
      - name: Image Digest
        run:  echo ${{ steps.build.outputs.digest }}
      
      - name: Update Downstream - zowe-v1-lts
        uses: benc-uk/workflow-dispatch@v1
        with: 
          ref:      refs/heads/master
          workflow: ci
          repo:     awharn/jenkins-nvm-zowe-cli-extended
          token:    ${{ secrets.PAT }}

  build-and-deploy-v2:
    runs-on: ubuntu-20.04
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Login to Dockerhub
        uses: docker/login-action@v2
        with:
          username: awharn
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push
        id:   build
        uses: docker/build-push-action@v4
        with:
          push: true
          build-args: |
            ZOWE_VERSION=zowe-v2-lts
          tags: |
            awharn/jenkins-nvm-zowe-cli:latest
            awharn/jenkins-nvm-zowe-cli:zowe-v2-lts
            ghcr.io/awharn/jenkins-nvm-zowe-cli:latest
            ghcr.io/awharn/jenkins-nvm-zowe-cli:zowe-v2-lts
      
      - name: Image Digest
        run:  echo ${{ steps.build.outputs.digest }}